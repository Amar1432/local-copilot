{
  "name": "@local-copilot/core",
  "version": "0.1.1",
  "private": true,
  "description": "Core completion engine and provider interfaces",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.1.0",
    "vitest": "^0.34.0",
    "tsup": "^8.0.0"
  },
  "dependencies": {
    "@local-copilot/shared": "workspace:*"
  }
}
